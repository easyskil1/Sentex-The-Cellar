{
  "name": "sentex-the-cellar",
  "version": "0.6.1",
  "description": "Sentex: The Cellar — Isaac-style top-down roguelike (Canvas + TypeScript)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
